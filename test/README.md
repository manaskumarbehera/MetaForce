# Tests

This folder holds lightweight test classes and scripts.

## Current test class
- `UrlParserTest.js` validates the Lightning record URL parsing pattern used by `contentScript.js`.

## Run tests
```bash
node test/UrlParserTest.js
```

